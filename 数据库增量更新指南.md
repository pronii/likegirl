# 📊 数据库增量更新指南

## 🎯 数据库结构变更总览

### 最近新增的数据库变更（需要同步）

#### 1. 音乐播放器功能（重要）
**文件**：`migration_add_music.sql`

**新增表**：`music` 表
```sql
CREATE TABLE IF NOT EXISTS `music` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL COMMENT '歌曲名称',
  `artist` varchar(100) DEFAULT NULL COMMENT '艺术家',
  `music_url` varchar(500) NOT NULL COMMENT '音乐文件URL',
  `cover_url` varchar(500) DEFAULT NULL COMMENT '封面图URL',
  `duration` int(11) DEFAULT 0 COMMENT '时长(秒)',
  `is_enabled` tinyint(1) DEFAULT 1 COMMENT '是否启用',
  `sort_order` int(11) DEFAULT 0 COMMENT '排序顺序',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_enabled_sort` (`is_enabled`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='音乐播放列表';
```

#### 2. 相册功能优化
**文件**：`migration_add_album_to_photos.sql`

**修改表**：`loveImg` 表新增字段
```sql
ALTER TABLE `loveImg` 
ADD COLUMN `album_id` int(11) NOT NULL DEFAULT '1' AFTER `id`;

CREATE INDEX `idx_album_id` ON `loveImg` (`album_id`);
```

#### 3. 缩略图支持（性能优化）
**文件**：`migration_add_thumbnail.sql`

**修改表**：`loveImg` 表新增字段
```sql
ALTER TABLE loveImg 
ADD COLUMN thumbUrl VARCHAR(255) DEFAULT NULL COMMENT '缩略图URL' AFTER imgUrl;
```

---

## 🚀 三种更新方式

### 方式一：宝塔面板图形化操作（推荐新手）

#### Step 1: 登录宝塔面板

1. 访问：`http://你的服务器IP:8888`
2. 输入用户名密码登录

#### Step 2: 进入数据库管理

1. 左侧菜单 → **数据库**
2. 找到你的数据库（例如 `likegirl`）
3. 点击 **管理** 按钮 → 进入 **phpMyAdmin**

#### Step 3: 执行 SQL 语句

**方法A：逐个执行**

1. 在 phpMyAdmin 中，左侧选择你的数据库
2. 顶部点击 **SQL** 标签
3. 复制粘贴以下 SQL（按顺序执行）：

##### ① 创建音乐表
```sql
-- 检查表是否存在
SELECT COUNT(*) FROM information_schema.tables 
WHERE table_schema = DATABASE() AND table_name = 'music';
-- 如果返回 0，执行下面的创建语句

CREATE TABLE IF NOT EXISTS `music` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL COMMENT '歌曲名称',
  `artist` varchar(100) DEFAULT NULL COMMENT '艺术家',
  `music_url` varchar(500) NOT NULL COMMENT '音乐文件URL',
  `cover_url` varchar(500) DEFAULT NULL COMMENT '封面图URL',
  `duration` int(11) DEFAULT 0 COMMENT '时长(秒)',
  `is_enabled` tinyint(1) DEFAULT 1 COMMENT '是否启用 1:启用 0:禁用',
  `sort_order` int(11) DEFAULT 0 COMMENT '排序顺序',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_enabled_sort` (`is_enabled`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='音乐播放列表';
```

4. 点击 **执行**，看到"查询成功"即完成

##### ② 添加相册字段（如果你有相册功能）
```sql
-- 检查字段是否存在
SELECT COUNT(*) FROM information_schema.columns 
WHERE table_schema = DATABASE() 
  AND table_name = 'loveImg' 
  AND column_name = 'album_id';
-- 如果返回 0，执行下面的添加语句

ALTER TABLE `loveImg` 
ADD COLUMN `album_id` int(11) NOT NULL DEFAULT '1' AFTER `id`;

CREATE INDEX `idx_album_id` ON `loveImg` (`album_id`);

-- 更新现有照片到默认相册
UPDATE `loveImg` 
SET `album_id` = 1
WHERE `album_id` = 0 OR `album_id` IS NULL;
```

##### ③ 添加缩略图字段（性能优化，可选）
```sql
-- 检查字段是否存在
SELECT COUNT(*) FROM information_schema.columns 
WHERE table_schema = DATABASE() 
  AND table_name = 'loveImg' 
  AND column_name = 'thumbUrl';
-- 如果返回 0，执行下面的添加语句

ALTER TABLE loveImg 
ADD COLUMN thumbUrl VARCHAR(255) DEFAULT NULL COMMENT '缩略图URL' AFTER imgUrl;
```

---

### 方式二：SSH 命令行执行（推荐熟手）

#### Step 1: 上传 SQL 文件到服务器

使用宝塔文件管理器上传以下文件到 `/tmp/` 目录：
- `migration_add_music.sql`
- `migration_add_album_to_photos.sql`
- `migration_add_thumbnail.sql`

#### Step 2: SSH 执行迁移

```bash
# 进入临时目录
cd /tmp

# 执行音乐表迁移
mysql -u你的数据库用户名 -p你的数据库名 < migration_add_music.sql

# 执行相册字段迁移
mysql -u你的数据库用户名 -p你的数据库名 < migration_add_album_to_photos.sql

# 执行缩略图字段迁移
mysql -u你的数据库用户名 -p你的数据库名 < migration_add_thumbnail.sql

# 验证
mysql -u你的数据库用户名 -p -e "SHOW TABLES LIKE 'music';" 你的数据库名
mysql -u你的数据库用户名 -p -e "DESCRIBE loveImg;" 你的数据库名
```

#### Step 3: 清理临时文件
```bash
rm -f /tmp/migration_*.sql
```

---

### 方式三：一键批量执行脚本（最快）

#### 创建合并的迁移 SQL

将以下内容保存为 `database_update_all.sql`：

```sql
-- ============================================
-- LikeGirl 数据库增量更新脚本
-- 执行前请先备份数据库！
-- ============================================

-- 开始事务（安全起见）
START TRANSACTION;

-- ============================================
-- 1. 创建音乐播放器表
-- ============================================
CREATE TABLE IF NOT EXISTS `music` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL COMMENT '歌曲名称',
  `artist` varchar(100) DEFAULT NULL COMMENT '艺术家',
  `music_url` varchar(500) NOT NULL COMMENT '音乐文件URL',
  `cover_url` varchar(500) DEFAULT NULL COMMENT '封面图URL',
  `duration` int(11) DEFAULT 0 COMMENT '时长(秒)',
  `is_enabled` tinyint(1) DEFAULT 1 COMMENT '是否启用 1:启用 0:禁用',
  `sort_order` int(11) DEFAULT 0 COMMENT '排序顺序',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_enabled_sort` (`is_enabled`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='音乐播放列表';

-- ============================================
-- 2. 相册功能：添加 album_id 字段
-- ============================================
-- 检查字段是否存在，不存在才添加
SET @column_exists = (
  SELECT COUNT(*) FROM information_schema.columns 
  WHERE table_schema = DATABASE() 
    AND table_name = 'loveImg' 
    AND column_name = 'album_id'
);

SET @sql = IF(@column_exists = 0, 
  'ALTER TABLE `loveImg` ADD COLUMN `album_id` int(11) NOT NULL DEFAULT 1 AFTER `id`', 
  'SELECT "album_id already exists"'
);
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;

-- 创建索引（如果不存在）
SET @index_exists = (
  SELECT COUNT(*) FROM information_schema.statistics 
  WHERE table_schema = DATABASE() 
    AND table_name = 'loveImg' 
    AND index_name = 'idx_album_id'
);

SET @sql = IF(@index_exists = 0, 
  'CREATE INDEX `idx_album_id` ON `loveImg` (`album_id`)', 
  'SELECT "idx_album_id already exists"'
);
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;

-- 更新现有照片到默认相册
UPDATE `loveImg` 
SET `album_id` = 1
WHERE `album_id` = 0 OR `album_id` IS NULL;

-- ============================================
-- 3. 缩略图支持：添加 thumbUrl 字段
-- ============================================
SET @column_exists = (
  SELECT COUNT(*) FROM information_schema.columns 
  WHERE table_schema = DATABASE() 
    AND table_name = 'loveImg' 
    AND column_name = 'thumbUrl'
);

SET @sql = IF(@column_exists = 0, 
  'ALTER TABLE `loveImg` ADD COLUMN `thumbUrl` VARCHAR(255) DEFAULT NULL COMMENT \'缩略图URL\' AFTER `imgUrl`', 
  'SELECT "thumbUrl already exists"'
);
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;

-- ============================================
-- 提交事务
-- ============================================
COMMIT;

-- ============================================
-- 验证结果
-- ============================================
SELECT '✅ 数据库更新完成！' AS status;

-- 检查新表
SELECT 
  CASE 
    WHEN COUNT(*) > 0 THEN '✅ music 表已创建'
    ELSE '❌ music 表创建失败'
  END AS music_table_status
FROM information_schema.tables 
WHERE table_schema = DATABASE() AND table_name = 'music';

-- 检查新字段
SELECT 
  CASE 
    WHEN COUNT(*) > 0 THEN '✅ album_id 字段已添加'
    ELSE '⚠️ album_id 字段不存在'
  END AS album_id_status
FROM information_schema.columns 
WHERE table_schema = DATABASE() 
  AND table_name = 'loveImg' 
  AND column_name = 'album_id';

SELECT 
  CASE 
    WHEN COUNT(*) > 0 THEN '✅ thumbUrl 字段已添加'
    ELSE '⚠️ thumbUrl 字段不存在'
  END AS thumbUrl_status
FROM information_schema.columns 
WHERE table_schema = DATABASE() 
  AND table_name = 'loveImg' 
  AND column_name = 'thumbUrl';
```

#### 执行一键脚本

**宝塔面板执行**：
1. 上传 `database_update_all.sql` 到站点根目录
2. 数据库 → 管理 → phpMyAdmin
3. SQL 标签 → 点击 **导入**
4. 选择文件 → 上传 `database_update_all.sql`
5. 点击 **执行**

**SSH 执行**：
```bash
mysql -u数据库用户名 -p数据库名 < database_update_all.sql
```

---

## ✅ 验证数据库更新

### 方法1：宝塔 phpMyAdmin 验证

1. 进入 phpMyAdmin
2. 选择你的数据库
3. 执行以下查询：

```sql
-- 检查 music 表
SHOW TABLES LIKE 'music';

-- 检查 loveImg 表结构
DESCRIBE loveImg;

-- 查看 music 表结构
DESCRIBE music;
```

**预期结果**：
- `music` 表存在 ✅
- `loveImg` 表有 `album_id` 字段 ✅
- `loveImg` 表有 `thumbUrl` 字段 ✅

---

### 方法2：SSH 命令验证

```bash
mysql -u数据库用户名 -p -e "
USE 数据库名;
SHOW TABLES LIKE 'music';
DESCRIBE loveImg;
SELECT COUNT(*) as music_count FROM music;
" 
```

---

## 🔄 更新失败回滚

如果更新出错，可以回滚：

### 删除新表
```sql
DROP TABLE IF EXISTS `music`;
```

### 删除新字段
```sql
ALTER TABLE `loveImg` DROP COLUMN IF EXISTS `album_id`;
ALTER TABLE `loveImg` DROP COLUMN IF EXISTS `thumbUrl`;
ALTER TABLE `loveImg` DROP INDEX IF EXISTS `idx_album_id`;
```

---

## ⚠️ 重要注意事项

### 1. 先备份数据库！
**宝塔面板备份**：
1. 数据库 → 找到你的数据库
2. 点击 **备份** 按钮
3. 等待备份完成（在备份列表查看）

### 2. 检查表是否已存在
如果你之前已经手动创建过这些表/字段，脚本会自动跳过（使用 `IF NOT EXISTS`）

### 3. 不要直接执行 `.gitignore` 排除的文件
以下文件是测试文件，**不要执行**：
- ❌ `migration_music_clean.sql`
- ❌ `fix_*.sql`
- ❌ `test_*.php`

### 4. 生产环境建议
- 先在测试环境执行
- 低峰期（凌晨）执行
- 执行前通知用户维护

---

## 📊 数据库变更总结

| 变更类型 | 表名 | 字段/操作 | 必须 | 说明 |
|---------|------|----------|-----|------|
| 新增表 | `music` | 整个表 | ✅ 是 | 音乐播放器功能 |
| 新增字段 | `loveImg` | `album_id` | ⚠️ 可选 | 相册分类功能 |
| 新增字段 | `loveImg` | `thumbUrl` | ⚠️ 可选 | 缩略图优化 |
| 新增索引 | `loveImg` | `idx_album_id` | ⚠️ 可选 | 查询性能优化 |

**如果你不需要音乐播放器功能**，可以跳过 `music` 表创建。

---

## 🚀 快速操作检查清单

更新前：
- [ ] 备份数据库（宝塔面板一键备份）
- [ ] 记录当前表结构（`DESCRIBE loveImg`）
- [ ] 确认数据库用户名和密码

更新中：
- [ ] 执行 SQL 脚本（选择上述三种方式之一）
- [ ] 查看执行结果（是否有报错）

更新后：
- [ ] 验证新表存在（`SHOW TABLES LIKE 'music'`）
- [ ] 验证新字段存在（`DESCRIBE loveImg`）
- [ ] 测试相关功能（音乐播放器、相册）

---

## 📞 遇到问题？

### 常见错误

**错误1**：`Table 'music' already exists`
```
解决：说明表已存在，跳过或先删除旧表
```

**错误2**：`Duplicate column name 'album_id'`
```
解决：字段已存在，跳过此步骤
```

**错误3**：`Access denied for user`
```
解决：检查数据库用户名密码是否正确
```

**错误4**：`Syntax error near 'ALGORITHM=INPLACE'`
```
解决：MySQL版本过低，移除 ALGORITHM 参数
```

---

## 📁 相关文件

- `migration_add_music.sql` - 音乐表创建
- `migration_add_album_to_photos.sql` - 相册字段
- `migration_add_thumbnail.sql` - 缩略图字段
- `database_update_all.sql` - 一键合并脚本（需创建）

---

**预计更新时间**：5-10 分钟  
**难度**：⭐⭐ (简单)  
**风险**：低（有回滚方案）

**建议先备份，再执行！** 🎯
