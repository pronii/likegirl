# 数据库问题修复说明

## 🐛 问题原因

之前创建的 `install_complete.sql` 文件中的字段名与原始代码不匹配：
- ❌ 错误：使用了 `username` 和 `password`
- ✅ 正确：应该使用 `user` 和 `pw`

## ✅ 解决方案

我已经创建了正确的合并文件：`install_all.sql`

这个文件包含：
1. ✅ 原始的 `love_db.sql`（完整的基础表和数据）
2. ✅ 相册分类功能（`love_album` 表 + `album_id`、`thumbUrl` 字段）
3. ✅ 音乐播放器（`music` 表 + `music_api_config` 表）

## 📋 重新导入步骤

### 方法一：phpMyAdmin（推荐）

1. **删除旧数据库**
   - 登录 phpMyAdmin
   - 选择 `likegirl` 数据库
   - 点击 "操作" 标签
   - 点击 "删除数据库(DROP)"
   - 确认删除

2. **创建新数据库**
   - 点击 "新建"
   - 数据库名：`likegirl`
   - 排序规则：`utf8mb4_general_ci`
   - 点击 "创建"

3. **导入新的SQL文件**
   - 选择刚创建的 `likegirl` 数据库
   - 点击 "导入" 标签
   - 选择文件：`D:\phpstudy_pro\WWW\install_all.sql`
   - 点击 "执行"

### 方法二：MySQL命令行

```sql
-- 删除旧数据库
DROP DATABASE IF EXISTS likegirl;

-- 创建新数据库
CREATE DATABASE likegirl CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

-- 选择数据库
USE likegirl;

-- 导入SQL文件
SOURCE D:/phpstudy_pro/WWW/install_all.sql;
```

## 🔑 登录信息

导入完成后：

- **前台地址**：http://localhost/
- **后台地址**：http://localhost/admin/
- **用户名**：admin
- **密码**：love2025
- **安全码**：851055720ccZ@（在 `admin/Config_DB.php` 中）

## 🗄️ 数据库结构

### 基础表（11个）
- `about` - 关于页面配置
- `article` - 文章/日记
- `diySet` - 自定义设置
- `IPerror` - IP黑名单
- `leavSet` - 留言设置
- `leaving` - 留言数据
- `login` - 管理员账号（字段：id, user, pw）
- `loveImg` - 相册图片（新增字段：album_id, thumbUrl）
- `lovelist` - 恋爱清单
- `text` - 网站基本信息（字段：id, boy, girl, title, logo...）
- `warning` - 安全日志

### 新增表（2个）
- `love_album` - 相册分类
- `music` - 音乐播放列表
- `music_api_config` - 音乐API配置

## ⚠️ 重要字段说明

### login 表
```
id (int)
user (varchar) - 用户名
pw (char(32)) - 密码（MD5加密）
```

### text 表
```
id (int)
boy (varchar) - 男主角昵称
girl (varchar) - 女主角昵称
title (varchar) - 网站标题
logo (varchar) - 网站Logo
writing (varchar) - 网站文案
boyimg (varchar) - 男生QQ号
girlimg (varchar) - 女生QQ号
startTime (varchar) - 恋爱开始时间
icp (varchar) - 备案号
Copyright (varchar) - 版权信息
... （还有更多字段）
```

## 🎯 导入后验证

导入成功后，你应该能够：

1. ✅ 访问前台首页看到网站内容
2. ✅ 访问后台登录页面
3. ✅ 使用 admin/love2025 成功登录
4. ✅ 在后台看到统计数据（留言、文章、清单数量）
5. ✅ 前台各个页面正常显示

## 🔧 如果还有问题

如果导入后仍然有问题，请检查：

1. **数据库配置文件**：`admin/Config_DB.php`
   ```php
   $db_address = "localhost";
   $db_username = "root";
   $db_password = "root";
   $db_name = "likegirl";
   ```

2. **PHP错误日志**
   - 查看 phpstudy 的日志文件
   - 或在浏览器中按 F12 打开开发者工具查看错误

3. **数据库连接**
   - 确认 MySQL 服务正在运行
   - 确认数据库名称正确

---

**文档更新时间**：2026-06-13
**问题修复状态**：✅ 已修复字段名不匹配问题
